mod catalog;
mod config;
mod error;
mod messages;
mod nostr;
mod persistence;
mod pipeline;
mod polling;
mod providers;
mod ratelimit;
mod state;

fn main() {
    println!("purser: scaffold only — see GitHub issues for implementation tracking");
}
