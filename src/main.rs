mod catalog;
mod config;
mod error;
mod messages;
mod nostr;
mod polling;
mod providers;
mod ratelimit;
mod state;

fn main() {
    println!("purser: scaffold only — see GitHub issues for implementation tracking");
}
